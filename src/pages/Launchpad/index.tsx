import React from "react";
import Content from "../../components/Content";

const Launchpad: React.FC = () => {
	return (
		<Content className="fade-in" key={"launchpad"}>
			Launchpad
		</Content>
	);
};

export default Launchpad;
